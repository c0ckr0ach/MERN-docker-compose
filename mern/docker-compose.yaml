version: '1.0'

services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - mongodb
  
  backend:
    build: ./backend
    ports:
      - "5050:5050"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: